[package]
name = "rewire-viewer"
version = "0.9.0"
edition = "2021"
license = "Apache-2.0"
description = "Rewire viewer based on Rerun API for bridge introspection"
keywords = ["ros2", "rerun", "robotics", "visualization", "rewire"]
categories = ["science::robotics", "visualization"]
repository = "https://github.com/rewire-run/viewer"

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "rewire-viewer"
path = "src/main.rs"

[features]
default = ["native"]
native = ["rewire-extras/grpc"]

[dependencies]
arrow = "58"
egui = "0.36"
egui_extras = "0.36"
re_chunk_store = "0.38"
re_entity_db = "0.38"
# re_viewer takes re_importer with default-features = false and exposes no way to
# turn the formats back on; depending on it directly unifies them back in.
re_importer = { version = "0.38", features = ["all"] }
re_log = "0.38"
re_log_types = "0.38"
re_sdk_types = "0.38"
re_ui = "0.38"
re_viewer = { version = "0.38", default-features = false }
re_viewer_context = "0.38"
rewire-extras = { git = "https://github.com/rewire-run/extras", tag = "v0.13.0", default-features = false }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
clap = { version = "4", features = ["derive"] }
eframe = "0.36"
mimalloc = "0.1"
re_async = "0.38"
re_crash_handler = "0.38"
re_grpc_client = "0.38"
re_grpc_server = "0.38"
re_log_channel = "0.38"
re_memory = "0.38"
re_uri = "0.38"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
eframe = { version = "0.36", default-features = false, features = ["wgpu"] }
wasm-bindgen = "0.2"
# Referenced by the `#[wasm_bindgen]` expansion of `async fn`, not by our source.
wasm-bindgen-futures = "0.4"
web-time = "1"
web-sys = { version = "0.3.77", features = [
  "DomException",
  "EncodedVideoChunk",
  "EncodedVideoChunkInit",
  "EncodedVideoChunkType",
  "HardwareAcceleration",
  "HtmlCanvasElement",
  "Navigator",
  "VideoDecoder",
  "VideoDecoderConfig",
  "VideoDecoderInit",
  "VideoFrame",
  "Window",
] }
